let str = "JavaScript";

// substring(start, end) → excludes end index
console.log(str.substring(0, 4)); // "Java"

// slice(start, end)
console.log(str.slice(4, 10)); // "Script"

// substr(start, length) (old, but still works)
console.log(str.substr(4, 6)); // "Script"
